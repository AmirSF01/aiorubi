import logging

dispatcher = logging.getLogger("aiorubi.dispatcher")
event = logging.getLogger("aiorubi.event")
middlewares = logging.getLogger("aiorubi.middlewares")
webhook = logging.getLogger("aiorubi.webhook")
scene = logging.getLogger("aiorubi.scene")